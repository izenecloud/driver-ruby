{
  "title":"Commands",
  "type":"map",
  "desc":"Sends index, mining, optimize_index commands.",
  "mapping": {
    "uri": {
      "title":"URI",
      "type":"str",
      "desc":"Which command?",
      "enum": ["commands/index", "commands/mining", "commands/optimize_index"],
      "required":true,
      "desc_enum": {
        "commands/index":"Ask SIA to index SCD files",
        "commands/mining":"Ask MIA to mining SCD files",
        "commands/optimize_index":"Ask SIA to optimize index files"
      }
    },

    "collection": {
      "title":"Collection Name",
      "type":"str",
      "desc":"Specifie a collection name",
      "required":true
    },

    "document_count": {
      "title":"Document Count",
      "type":"int",
      "desc":"Index at most so many documents in each SCD file. It is for developer."
    }
  }
}
