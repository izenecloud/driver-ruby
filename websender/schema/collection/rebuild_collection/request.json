{
  "title":"Create document",
  "type":"map",
  "desc":"Rebuild collection to Clear these deleted documents. DOCID is required.",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specify a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    } 
  }
}
