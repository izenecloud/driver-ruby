{
  "title":"Similar Documents",
  "type":"map",
  "desc":"Search documents similar to specified target document",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specify a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "search_session": {
      "type":"map",
      "desc": "Trace last search. Can be used to hightlight and other features.",
      "mapping": $(search)
    },

    "duplicate_with": {
      "type": "map",
      "desc": "Add exactly one DOCID or _id",
      "required": true,
      "mapping": {
        "_id": {
          "type":"int",
          "desc":"Specify internal ID of the target document."
        },
        "DOCID": {
          "type":"str",
          "desc":"Specify user specified DOCID of the target document."
        }
      }
    },

    "limit": {
      "type": "int",
      "desc": "Number of documents in page"
    },

    "offset": {
      "type": "int",
      "desc": "Offset of documents in current page"
    },

    "select": {
      "type": "seq",
      "sequence": $(select)
    }
  }
}
