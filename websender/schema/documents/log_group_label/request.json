{
  "title":"Log group label",
  "type":"map",
  "desc":"Increase the frequency count of clicked group label.",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specify a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "resource": {
      "type":"map",
      "mapping": {
        "keywords": {
          "type": "str",
          "required": true,
          "desc": "user query"
        },
        "group_property": {
          "type": "str",
          "required": true,
          "desc": "the group property name"
        },
        "group_label": {
          "type": "seq",
          "required": true,
          "desc": "the group label path",
          "sequence": [{
            "type": "str",
            "desc": "the node value"
          }]
        }        
      },
      "desc":"Set the most frequently clicked group label",
      "required":true
    }
  }
}
