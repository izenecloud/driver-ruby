{
  "title":"Faceted Representation",
  "type":"map",
  "desc":"static global faceted representation.",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specifie a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    }
  }
}
