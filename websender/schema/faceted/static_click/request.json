{
  "title":"Faceted Click",
  "type":"map",
  "desc":"static click on the faceted categories",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specifie a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "category_id": {
      "type":"int",
      "desc":"The cateogory id.",
      "required":true
    }
  }
}
