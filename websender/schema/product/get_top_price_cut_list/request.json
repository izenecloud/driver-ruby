{
  "title":"get_multi_price_history",
  "type":"map",
  "desc":"get_multi_price_history",
  "mapping": {

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "collection": {
      "type": "str",
      "desc": "collection name",
      "required": true
    },

    "property": {
      "type": "str",
      "desc": "property name",
      "required": true
    },

    "value": {
      "type": "str",
      "desc": "property value",
      "required": true
    },

    "days": {
      "type": "int",
      "desc": "time interval",
      "required": true
    }
  }
}
