{
  "title":"append_to_group",
  "type":"map",
  "desc":"append_to_group.",
  "mapping": {

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "collection": {
      "type": "str",
      "desc": "collection name",
      "required": true
    },

    "uuid": {
      "type": "str",
      "desc": "uuid",
      "required": true
    },

    "docid_list": {
      "type": "seq",
      "desc": "docid_list",
      "sequence": [{
        "type": "int",
        "title": "docid"
      }]
    }
  }
}
