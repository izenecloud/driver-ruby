{
  "title":"Get Item",
  "type":"map",
  "desc":"get item attributes by item id.",
  "mapping": {
    "collection": {
      "type":"str",
      "desc":"Specify a collection name",
      "required":true
    },

    "header": {
      "type":"map",
      "mapping": $(header)
    },

    "resource": {
      "type":"map",
      "mapping": {
        "ITEMID": {
          "type": "str",
          "required": true,
          "desc": "the unique item id"
        }
      },
      "desc":"Specify item id",
      "required":true
    }
  }
}
